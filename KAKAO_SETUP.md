# 카카오톡 챗봇 연동 설정 가이드

앱 코드(웹훅, 명령어 파서, 연동 화면)는 이미 다 구현되어 있어요. 이 문서는 카카오 쪽에서
직접 해야 하는 설정만 정리한 거예요 — 전부 카카오 계정으로 로그인해서 웹 UI에서 하는
작업이라 대신 해드릴 수 없어요.

## 0. 미리 준비할 것

- 카카오 계정 (개인 계정이면 충분해요, 별도 사업자 등록 필요 없음)
- 이 앱이 배포된 Vercel URL (예: `https://your-app.vercel.app`)

## 1. 카카오톡 채널 만들기

1. https://center-pf.kakao.com 접속 → 로그인 → **채널 만들기**
2. 이름/소개 등 적당히 입력하고 생성 (검색 노출 여부는 "노출 안 함"으로 해도 무방 — 어차피
   본인만 쓸 거라면 채널 링크로 카카오톡에서 직접 추가하면 돼요)

## 2. 카카오 i 오픈빌더에서 챗봇 만들기

1. https://i.kakao.com 접속 → 로그인 → **오픈빌더** → **봇 만들기**
2. 봇 이름 입력 후 생성
3. 왼쪽 메뉴 **배포 → 채널 연결**에서 1단계에서 만든 채널을 연결

## 3. 스킬(웹훅) 등록

1. 왼쪽 메뉴 **스킬** → **스킬 추가**
2. URL에 아래를 입력 (SECRET은 직접 정한 임의의 긴 문자열 — Vercel 환경변수
   `KAKAO_WEBHOOK_SECRET`에 넣을 값과 동일해야 해요):
   ```
   https://your-app.vercel.app/api/kakao-webhook?secret=SECRET
   ```
3. 저장

## 4. 폴백 블록에 스킬 연결

새로 만든 봇은 기본적으로 **폴백 블록**(다른 블록에 안 걸리는 모든 발화가 여기로 옴) 하나만
있어요 — 이 앱은 자체적으로 모든 명령어를 텍스트로 파싱하므로, 카카오 쪽에는 패턴 블록을
여러 개 만들 필요 없이 **폴백 블록 하나만 스킬에 연결하면 끝**이에요.

1. 왼쪽 메뉴 **시나리오 → 폴백 블록** 클릭
2. **응답** 섹션에서 기존 "죄송합니다..." 같은 텍스트 응답을 삭제
3. **스킬 데이터 사용** 켜기 → 3단계에서 만든 스킬 선택
4. 저장

(웰컴 블록은 그대로 둬도 되고, 원한다면 "카카오톡 채팅으로 날씨, 오늘 일정, 지출/일정/연습/운동
기록을 남길 수 있어요. 먼저 앱 설정에서 연동 코드를 받아 '연동 123456'처럼 보내주세요"
같은 안내 문구로 바꿔도 좋아요.)

## 5. 봇 테스트 (배포 전 확인)

오픈빌더 오른쪽의 **봇 테스트** 패널에서 바로 대화해볼 수 있어요. "날씨"라고 쳐보고
연동 안내 메시지가 오는지 확인하세요 (아직 연동 전이라 정상이에요).

## 6. Vercel 환경변수 추가

Vercel 프로젝트 설정 → Environment Variables에 추가:

| 변수명                 | 값                                          |
| ----------------------- | -------------------------------------------- |
| `KAKAO_WEBHOOK_SECRET` | 3단계에서 URL에 넣은 것과 동일한 임의의 긴 문자열 |

`FIREBASE_SERVICE_ACCOUNT`는 이미 설정되어 있을 거예요 (푸시 알림 기능이 쓰던 것과 같은
서비스 계정을 그대로 씁니다).

환경변수를 추가/변경한 뒤에는 **`npx vercel --prod`로 다시 배포해야 반영돼요** — `git push`만
으로는 배포되지 않아요.

## 7. 앱에서 연동하기

1. 이 앱 접속 → **설정 → 카카오톡 연동** → **연동 코드 받기** (6자리 숫자, 10분간 유효)
2. 카카오톡에서 1단계 채널을 추가하고 채팅방을 연 뒤, **"연동 123456"**처럼 코드를 보내기
3. "연동됐어요!" 응답이 오면 완료

## 8. 사용할 수 있는 명령어

| 보낼 메시지 예시                     | 동작                                  |
| -------------------------------------- | --------------------------------------- |
| `날씨` / `오늘 날씨 어때`             | 오늘 날씨 + 유의사항 (비/눈/폭염 등)   |
| `오늘 일정` / `오늘의 일정`           | 오늘 등록된 일정 목록                  |
| `지출 12000 식비 점심값`              | 가계부에 지출 12,000원 (식비, 메모: 점심값) 기록 |
| `수입 5만 용돈`                       | 가계부에 수입 50,000원 (용돈) 기록     |
| `일정추가 8/20 치과 예약`             | 8/20에 "치과 예약" 일정 추가 (날짜 생략 시 오늘) |
| `베이스연습 스케일 30분`              | 오늘 날짜로 연습 기록 추가             |
| `운동 가슴`                           | 오늘 날짜로 가슴 운동 기록 (하루 1회)  |
| `도움말`                              | 전체 명령어 안내                       |

지출/일정추가의 날짜는 `오늘`/`내일`/`모레`/`어제`, `8/20`, `8.20`, `8월20일` 형식을
알아들어요. 지출 금액은 `12000`, `12,000`, `1만2천`, `1.5만`처럼 입력해도 돼요.

카테고리(`식비` 등)·운동 부위(`가슴` 등)는 앱에서 실제로 등록된 이름과 매칭돼요 — 등록되지
않은 이름을 입력하면 지출은 첫 번째 카테고리로, 운동은 실패 메시지로 처리돼요. 앱의
카테고리 관리 화면에서 먼저 이름을 확인해두면 정확히 매칭돼요.

## 참고: 동작 원리

카카오톡에서 온 메시지는 이 앱의 다른 기기와 똑같은 방식으로 동기화돼요 — 서버가 Firestore의
같은 계정 데이터에 직접 기록하고, 앱을 열어둔 다른 기기는 실시간으로 그 변경을 받아
merge해요. 즉 카카오톡은 "또 하나의 기기"처럼 동작하는 셈이라, 앱을 다시 켤 필요 없이
그대로 반영돼요.
